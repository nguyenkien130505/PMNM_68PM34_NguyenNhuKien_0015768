RewriteEngine On
RewriteBase /
Options -Indexes

# Tránh rewrite lại index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Chuyển hướng về index.php, gán giá trị vào biến url VÀ giữ lại các tham số phía sau
RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]